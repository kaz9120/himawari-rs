[package]
name = "himawari-core"
description = "盤面表現・指し手生成・SFEN入出力（探索非依存）"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
# 入力をHalfKAへ拡張する（相手玉の81升平面、ADR-0193）。
# 特徴次元が変わるためネットの互換が切れる。既定はHalfKP。
halfka = []

[lints]
workspace = true

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "bb_layout"
harness = false

[[bench]]
name = "effect_gen"
harness = false
