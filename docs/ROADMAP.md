# ロードマップ

着手を決めるときに読む文書。現在地・次の方向・未着手の候補を持つ。

なぜそうしたかは[ADR索引](adr/README.md)、何が入ったかは
[CHANGELOG.md](../CHANGELOG.md)にある。この文書に過去の経緯は書かない。
着手を決めた案はADRを起こし、候補から消す。

## 現行構成

| 項目 | 実体 |
|---|---|
| エンジン | `target/release/himawari`（FT256、FT重みi8。[ADR-0138](adr/0138-ft-i8-quantization.md)） |
| 評価関数 | `data/nets/halfkp_2990M_q1.hmwr.best`（32.1MB。29.9億局面の静止化教師で学習。[ADR-0135](adr/0135-teacher-data-3b.md)・[ADR-0136](adr/0136-quiet-teacher-positions.md)） |
| 定跡 | `data/book/main.db`（1250局面・4710手、深さ28。相手の初手を全網羅。[ADR-0146](adr/0146-book-full-width-opening.md)、実戦分の追加は[ADR-0152](adr/0152-floodgate-cycle.md)） |
| floodgate | レート3484（562局、296勝266敗。2026-08-14時点） |

2026-07-24の初参戦は30局でレート3186だった。当時「実力の壁」と見ていた
3100〜3250は越えている。目標は2027年5月の世界コンピュータ将棋選手権で、
参加時のビルドを1.0.0とする（[ADR-0068](adr/0068-sprt-driven-versioning.md)）。

上位帯には届いていない。レート4000超の6エンジンには64局で1勝しかしておらず、
3700〜3900台が相手でも勝率は2割前後にとどまる。

### 制約

- 公開教師データを使い切った。hao_depth9は381ファイル・約30.0億局面すべてを
  取得済みで、次の増量は自前生成しかない（[ADR-0144](adr/0144-selfplay-teacher-loop.md)）
- 学習の所要が規模で効く。RAM 48GBに対し29.9億のpsvは111GBあり、
  ページキャッシュに載らない。処理速度は19.9億の222k局面/秒から96k局面/秒へ落ちる
- 持将棋のルールが2028年の選手権から27点法→24点法へ変わる見込み
  （2026-07-20オーナー共有）。影響するのは宣言勝ちの判定
  （[ADR-0030](adr/0030-nyugyoku-declaration.md)）、自前生成の勝敗ラベル、
  selfplayの入玉局面裁定の3か所である
- x86_64でのperft・ベンチ再計測は環境入手待ちで持ち越している

## 次の方向

### 1. 参照実装との残りの乖離を閉じる

参照追従の線はほぼ閉じている。定数は220項目超が一致しており、構造の整列は
群1〜3が3030局で−6.7として棄却された（[ADR-0155](adr/0155-reference-walkthrough.md)）。
個別に棄却した5件をまとめ直す案も、5件ともADR-0109の群で導入済みと分かって
棄却した（[ADR-0160](adr/0160-revisit-rejected-under-better-eval.md)）。

並列まわりの3件も閉じた（[ADR-0162](adr/0162-parallel-group.md)）。historyの
スレッド共有・best thread voting・窓幅多様化を1群で測り、`Threads=4` の
6398局で非劣性のH1を得た（+7.9 [−0.2, +16.1]）。TTクラスタの手番分割は、
本エンジンが64bit全体を照合するため不要と判断して見送った。

残るのはADR-0155の群4である。千日手判定を参照のdo_move前計算方式へ
置き換える。現状は走査に16手キャップがなく、200手級の対局では毎ノード
約20KBのストライド走査になる。性能バグを兼ねるので速度側からも測れる。
群5（rootの統合）と群6（小物）は優先度を下げたままにする。

### 2. FT1024の速度を取り返す

FT幅1024はノード数固定で+70.6 Elo [+49.2, +92.5] を得た
（[ADR-0159](adr/0159-ft-width-1024.md)）。評価関数としての実力は上がる。
ただし速度が0.709倍に落ち、10+0.1で−21.0、60+0.6でも−14.0にとどまった。
速度の代償は持ち時間にほぼ依存しない。単体では採択せず、保留している。

この測定で換算率が手に入った。速度1%あたり3.5 Elo（10+0.1）である。
速度改善の期待値を着手前に見積もれるようになった。候補は出力バケット
（[ADR-0137](adr/0137-output-buckets.md)）、玉位置キャッシュの続き
（[ADR-0156](adr/0156-bucket-accumulator-cache.md)）、利きの差分テーブル
（[ADR-0148](adr/0148-effect-table.md)）である。

挙動を変えない高速化は一巡した。第2弾で12群が入り、着手前main比で
+22.60% NPSになった（[ADR-0151](adr/0151-speedup-sweep.md)）。残るのは
`Threads=4` で測る群Eだけである。

### 3. 教師データを自前で作る

第1世代は−35.7 Eloで棄却した（[ADR-0144](adr/0144-selfplay-teacher-loop.md)）。
生成の仕組みは動き、5228万局面を3時間で作れる。足りないのは質である。
現行ネットは29.9億のhao_depth9で学習済みで、そこへ自分のdepth 8探索を
教え直しても情報が増えない。教師signalと静的評価の乖離は338で、haoの353を
下回る。

残る手は4つある。探索を深くする、開始局面を定跡から取る、hao 29.9億との
混合再学習にする、外部データを探す。深さを上げるのが最初の候補だが、
depth 9ぶんの差をどの深さで埋められるかは分かっていない。

### 4. 評価関数と探索を結合する

多ヘッド出力（WDL・進行度・安定度）を学習し、時間管理・枝刈りの強度・
contemptへ供給する（[ADR-0120](adr/0120-after-parity.md)）。追従で探索は
参照実装と同じ構造になった。同等より先へ行くには別の軸が要る。評価関数を
自前で学習していることが本エンジンの独自資産で、参照実装が持たない領域である。

工程が長い。学習器・ネット・探索の3つが揃って初めて測れるので、
1群1SPRTのような短い周期にならない。

### 5. 実戦を定点観測する

floodgateの棋譜を定期回収し、分析と定跡追加を決定論の手順で回す
（[ADR-0152](adr/0152-floodgate-cycle.md)）。回収済みは300局（2026-08-08まで）で、
wdoor側の562局に追いついていない。次の回収で差分を取り込む。

## 候補

未着手の案を1案1行で置く。着手を決めたらADRを起こしてここから消す。
完了・棄却した案も残さない（経緯と結果はADRが持つ）。

検証手段の略記は、SPRT=対局ゲート、loss=検証損失、NPS=速度計測とする。
表の並び順は優先度を表さない。

単発の候補は[ADR-0089](adr/0089-improvement-criteria.md)の3軸（速度・
ノード効率・終盤の正確さ）で選ぶ。どの軸に乗るかと、単独で効く仮説を
書けない候補は着手しない。参照実装に対応物がある候補はこの判断を使わず、
群単位で移す（[ADR-0109](adr/0109-reference-parity.md)）。

### 探索

過去にSPRTで棄却した案のうち、P3期（駒割評価）の測定は条件が違う。
singular extensionがP3の−16.0からP8で+12.6へ反転した先例があり、
NNUE後の再測定に意味がある。

| 案 | 狙い・メモ | 検証 |
|---|---|---|
| mate_1plyの見逃し率改善 | 見逃しは近傍のみで10.4%、遠隔駒ありで16.0%。原典は遠打ちの合駒不能判定や王手クラスをより広く扱う。通常探索への組み込みは[ADR-0139](adr/0139-mate1ply-in-search-retry.md)で棄却したので、終盤の詰み逃しに効くのはこの見逃し率と[ADR-0142](adr/0142-dfpn-mate-search.md)のdf-pnになる | SPRT |
| SEE駒価値のNNUE時代適合 | 枝刈り閾値系の駒価値を再調整する。[ADR-0143](adr/0143-spsa-tuning.md)のSPSA対象に載せる手もある | SPRT |

### 評価関数

| 案 | 狙い・メモ | 検証 |
|---|---|---|
| PSQT直結パス（material head） | FTから評価へのskip。序盤の材料感を安定化する | loss+SPRT |
| 手番特徴の明示化 | HalfKPは手番を視点でしか持たない。tempo項を学習する | loss |
| 合法手集合の予測 | [ADR-0133](adr/0133-effect-pretraining.md)の次の的。遮りに加えてピンと王手放置が絡むので非加法性が強く、2187次元と的も大きい。局面生成器がそのまま使える。利きが−0.001止まりだった理由が「的が細い」なら、ここで差が出る | probing+SPRT |
| 評価値のクランプ | 現状は±31871で実質無制限。外挿された極端な値がそのまま出る。2026-08-05のfloodgate戦で、詰まされる3手前から+1698→+5449→+6104と外挿しながら1手詰めを見落とした。この値域では枝刈りのマージンが通常の評価値域を前提にしているので、詰ます手ごと刈る筋を疑っている | 要検討 |
| i8とクリップ制約の内訳を切り分ける | [ADR-0138](adr/0138-ft-i8-quantization.md)の+29.8 Eloの出どころ。見積もりではi8の寄与は+1.3で、残りはクリップ制約つき学習から来ている。クリップが本体ならFT1024へi8を持ち込む価値は薄い。同一ビルドで `halfkp_2990M_ftclip` 対 `halfkp_2990M_fact` を測る | SPRT |
| 入玉局面の評価精度を測る | 教師データ（hao_depth9）は他エンジン由来で入玉が薄い。ただし実戦で問題が出た証拠はまだない | 実測 |

### 学習

| 案 | 狙い・メモ | 検証 |
|---|---|---|
| λのply依存化 | 序盤はresult重視、終盤はscore重視の混合比にする | loss+SPRT |
| 詰みスコアの扱い | ±30000近傍のclamp・除外・専用ターゲット。現状は8.5%を素通ししている | loss+SPRT |
| EMA重み平均（SWA） | 終盤の重み振動を平均化する。ほぼ無料で数Elo | SPRT |
| 継続学習の世代運用 | 前世代ネットからfinetuneする。lr小さめで積む | SPRT |
| 教師との指し手一致率の計測 | PSVのmoveフィールドを使う。lossと別の健全性指標になる | 指標 |
| 複数データセット混合 | hao系＋水匠系など。分布の偏りを緩和する（ライセンス確認） | SPRT |
| 序中終盤のサンプリング重み | gamePly別の採択率を調整する | loss+SPRT |
| 公開ネットウォームスタート対照 | [ADR-0039](adr/0039-trainer-v1.md)の対照実験。互換ローダで初期化する | SPRT |
| ハイパラのSPRT運用 | lr・λ・epochsを1変更=1SPRTで積む | SPRT |

### 基盤

| 案 | 狙い・メモ | 検証 |
|---|---|---|
| 学習チェックポイント | f32状態の保存・再開。長時間学習の中断耐性を上げる | 動作 |
| 大規模対局基盤 | SPRTの並列度向上と複数マシン分散。[ADR-0143](adr/0143-spsa-tuning.md)・[ADR-0144](adr/0144-selfplay-teacher-loop.md)の消費量を支える | 運用 |
