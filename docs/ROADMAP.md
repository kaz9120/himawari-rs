# ロードマップと現在地

このプロジェクトの「やりたいこと・状況」はすべてこの文書で管理する。
GitHub Issuesは使わない。設計判断は [ADR索引](adr/README.md)、
未起草の設計論点はADR索引のバックログにある。
計測・検証の結果は [RESULTS.md](RESULTS.md)、改善アイデアは
[IDEAS.md](IDEAS.md)、教師データの所在は [DATASETS.md](DATASETS.md) を参照。
更新タイミングは、フェーズの出入り・残作業の増減があったとき。

## 現在地

- バージョン: 0.6.0（P5出口。2026-07-20オーナー承認）
- フェーズ: **P8進行中（構造の暫定フリーズを解除し、FT次元を再検討）**

構造は純粋HalfKP（180Mネット、対駒割+528）で暫定フリーズ済み。
2026-07-21〜25の探索改善キャンペーンで8件を採択し、単純加算で
約+544 Eloを積んだ（correction history +44.6、continuation
history +20.7、eval hash +54.1、singular +12.6、ProbCut +44.2、
qsearch TT +113.6、razoring +184.8、思考時間の難易度スケール +69.3。
詳細は[RESULTS.md](RESULTS.md)）。

2026-07-24〜25にfloodgateへ初参戦した（30局19勝11敗、レート3186）。
棋譜の集計で2点が分かった。負けはすべて相手レート3121以上で、実力の
境界が3100〜3250にある。負け11局はすべて長手数（105手以上）で、終盤
10手の消費が7〜9秒に落ちている。持ち時間の枯渇であり、時間配分の
設計が原因（[ADR-0059](adr/0059-easy-move-scaling.md)で対処）。

2026-07-25に学習データを370M局面へ拡大し、対halfkp_180Mで
**+161.6 Elo**（H1採択）を得た。

2026-07-26に学習基盤を高速化し（41,000→449,000 samples/s、
[ADR-0064](adr/0064-dense-ft-gradient-mps.md)・
[ADR-0065](adr/0065-large-scale-dataloader.md)）、データを19.9億局面へ
拡大した。対halfkp_370Mで**+243.8 Elo**、factorizer
（[ADR-0066](adr/0066-halfkp-factorizer.md)）でさらに**+28.1 Elo**を
得た（いずれもH1採択）。

ここでモデルの制約が変わった。train lossは0.48580から0.48652へ動かず、
汎化ギャップは0.0237から0.0082へ縮んだ。過学習は解消し、容量律速に
移っている。データを足しても改善の上限はtrain lossで頭打ちになるため、
構造の暫定フリーズを解いてFT次元の拡大へ進む
（[ADR-0067](adr/0067-ft-dimension-512.md)）。

### 現行の最強構成

| 項目 | 実体 |
|---|---|
| エンジン | FT256ビルド（`data/bin/himawari-ft256`） |
| 評価関数 | `data/nets/halfkp_1900M_fact.hmwr.best`（valid loss 0.49513） |

FT512は評価精度で上回る（valid loss 0.49374、train loss 0.48338）が、
NPSが0.65倍に落ちる代償を取り返せず、SPRTで-72.8 Elo（968局、H0採択）
だった（[ADR-0067](adr/0067-ft-dimension-512.md)）。比較を続けられるよう、
512のバイナリ（`data/bin/himawari-ft512`）とネット
（`data/nets/halfkp_1900M_ft512.hmwr.best`）も残してある。

## フェーズ進捗

| フェーズ | 内容 | 出口条件 | 状況 |
|---|---|---|---|
| P0 | リポジトリ基盤・横断方針 | CIが緑、ADRプロセス稼働 | 完了 |
| P1 | 盤面表現・指し手生成 | perft(5) = 19,861,490 がCIで緑 | 完了（v0.2.0） |
| P2 | USI + 探索v1（駒割評価） | GUIで対局完走、詰将棋正答 | 完了（v0.3.0） |
| P3 | 探索強化 + Lazy SMP + ルール完全対応 | NPSスケーリング確認、SPRTゲート運用開始 | 完了（v0.4.0） |
| P4 | NNUE推論 | 差分計算=全計算の一致、SIMD=スカラー一致 | 完了（v0.5.0） |
| P5 | 学習パイプライン | 自前学習ネットが駒割にSPRT有意勝ち | 完了（v0.6.0） |
| P6 | 学習基盤の完成 | データ量に対してvalid lossとEloが単調改善、ハイパラ固定 | 完了 |
| P7 | ネットワーク構造の探索・決定 | 構造をフリーズし、本格学習に移行 | FT512はADR-0067で不採択。純粋HalfKP 256x2-32-32を維持 |
| P8 | 本格学習 + 探索改善 + データ拡大 | floodgate参戦可能な棋力 | 進行中（19.9億局面で+243.8 Elo、探索改善キャンペーンも継続） |

バージョン運用（ADR-0007）: フェーズ出口ごとに0.MINORを上げてタグを打つ。

## 直近の残作業

- 定跡（[ADR-0063](adr/0063-book-loader-and-mini-book.md)）は実装・生成
  ともに完了（133局面）。floodgateへ投入して実戦で効果を見る。
  `BookFile` の指定が要る（既定は定跡なし）
- 時間管理の残り: 配分式そのものの再設計はIDEAS.mdの
  「時間配分式の再設計」に残した。やねうら王のmove_horizon方式が参考
- 容量を保ったままNPSを取り戻す。FT次元の拡大
  （[ADR-0067](adr/0067-ft-dimension-512.md)）は容量律速の診断を裏づけた
  一方、NPS 0.65倍の損で不採択になった。次はFT重みのi8量子化
  （IDEAS.mdの「量子化の再検討」）でメモリ帯域を半減させ、512の容量を
  活かせるかを試す
- 探索改善キャンペーンの継続（2026-07-23オーナー決定）:
  運用は従来どおり1アイデア1ADR・チューニングなし・SPRTゲート
  （[CLAUDE.md](../CLAUDE.md)参照）。次の候補はIDEAS.mdの探索節から
  選ぶ（SEE枝刈り再挑戦、mate1plyテーブル駆動版、aspiration窓など）
- ADR-0052（NMP動的化）は保留で確定（2026-07-23オーナー決定）。
  実装はadr-0052-wipブランチ。チューニング段階で再訪する
- データの残り: hao_depth9のstart_time=1695872823（127ファイル、
  約10億局面）が未取得。29.9億まで増やせるが、容量律速のうちは
  伸びが逓減する。FT拡大の後に判断する
- v0.7.0タグはhalfkp_1900M_factの採択で条件を十分に満たした。
  FT次元の判定も済んだので、いつでも打てる
- 持ち越し: x86_64でのperft・ベンチ再計測（環境入手待ち）、
  df-pn（任意）
