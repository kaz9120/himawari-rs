# ロードマップと現在地

このプロジェクトの「やりたいこと・状況」はすべてこの文書で管理する。
GitHub Issuesは使わない。設計判断は [ADR索引](adr/README.md)、
未起草の設計論点はADR索引のバックログにある。
更新タイミングは、フェーズの出入り・残作業の増減があったとき。

## 現在地

- バージョン: 0.3.0（P2出口。最初の対外リリース）
- フェーズ: **P3進行中**。SPRT対局基盤（ADR-0027、`selfplay`）は
  実装済み。以後の探索変更は「1機能=1SPRT」で積む
- 棋力: 駒割評価を考慮しても弱すぎる（2026-07-18の対局所感）。
  評価関数でなく探索能力の問題であり、このままNNUE化しても
  効果は限定的。**P3の探索強化がプロジェクト全体の最重要フェーズ**

## フェーズ進捗

| フェーズ | 内容 | 出口条件 | 状況 |
|---|---|---|---|
| P0 | リポジトリ基盤・横断方針 | CIが緑、ADRプロセス稼働 | 完了 |
| P1 | 盤面表現・指し手生成 | perft(5) = 19,861,490 がCIで緑 | 完了（v0.2.0） |
| P2 | USI + 探索v1（駒割評価） | GUIで対局完走、詰将棋正答 | 完了（v0.3.0） |
| P3 | 探索強化 + Lazy SMP + ルール完全対応 | NPSスケーリング確認、SPRTゲート運用開始 | 未着手 |
| P4 | NNUE推論 | 差分計算=全計算の一致、SIMD=スカラー一致 | 未着手 |
| P5 | 学習パイプライン | 自前学習ネットが駒割にSPRT有意勝ち | 未着手 |

バージョン運用（ADR-0007）: フェーズ出口ごとに0.MINORを上げてタグを打つ。
P2出口 = 0.3.0（最初の対外リリース候補）、P5出口 = 1.0.0。

## 直近の残作業

- [x] 互角局面集の入手（たややん互角局面集。`openings/` に配置、
      リポジトリには含めない。24手目版30,053局面・32手目版26,273局面）
- [ ] 枝刈り・延長パッケージのADR起草（NMP/LMR等。以後1機能=1SPRT）
- [ ] 外部の5手詰めセットでの正答率確認（P2持ち越し。セット入手次第。
      任意。`tsume --file`、1行 `SFEN,手数` 形式）

selfplayの検証状況: 同一エンジンの帰無検定（互角局面集・nodes固定）で
全ペアが正確なミラーになり、H0採択で終了することを確認済み
（usinewgameのTT全クリアで担保）。SPRT運用の準備は完了。

## P3以降の概要

各フェーズの設計論点は [ADR索引のバックログ](adr/README.md#バックログ) を
正とする。要点だけ再掲する。

- P3: SPRT対局基盤を最初に作り、以後の変更を「1機能=1SPRT」で積む。
  Lazy SMP、枝刈り（NMP/LMR/singular等）、mate1ply、入玉宣言勝ち、
  MultiPV・ponder。x86_64でのperft・ベンチ再計測（ADR-0010/0011の
  見直しトリガー）もここで行う
- P4: NNUE推論（HalfKP系）。既存公開評価関数の読み込み互換を検討し、
  推論部と学習部のバグを切り分ける
- P5: gensfen・学習器・世代ループ。学習の再現条件とネットワーク識別子の
  管理（ADR-0007の残課題）

## 実測の記録

性能・検証の代表値。環境は Apple Silicon、`-C target-cpu=native`。

| 項目 | 値 | 時点 |
|---|---|---|
| perft(5) / perft(6) | 19,861,490 / 547,581,517（公開値一致） | P1出口 |
| perftのNPS（bulk） | 約1.2億 | P1出口 |
| 探索NPS（駒割評価・単スレッド） | 約1,100万 | P2実装完了時 |
| 平手1秒の到達深さ | depth 10 | P2実装完了時 |

P3探索強化のSPRT記録（ADR-0028。条件は10+0.1秒、elo[0,5]、α=β=0.05）。

| 変更 | 結果 | Elo | 局数 |
|---|---|---|---|
| 静的評価TT保存+NMP | H1採択 | +103.4 [+76.7,+131.4] | 588 |
| LMR | H1採択 | +155.1 [+120.1,+193.4] | 432 |
| futility / reverse futility | H1採択 | +86.8 [+62.7,+111.7] | 752 |
