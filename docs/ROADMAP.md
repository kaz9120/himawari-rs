# ロードマップと現在地

このプロジェクトの「やりたいこと・状況」はすべてこの文書で管理する。
GitHub Issuesは使わない。設計判断は [ADR索引](adr/README.md)、
未起草の設計論点はADR索引のバックログにある。
計測・検証の結果は [RESULTS.md](RESULTS.md)、改善アイデアは
[IDEAS.md](IDEAS.md)、教師データの所在は [DATASETS.md](DATASETS.md) を参照。
更新タイミングは、フェーズの出入り・残作業の増減があったとき。

## 現在地

- バージョン: 0.6.0（P5出口。2026-07-20オーナー承認）
- フェーズ: **P7出口判定済み（構造を純粋HalfKPで暫定フリーズ）→ P8先行着手**

P7で利き塔を除去（NPS +40%）し、180Mデータの純粋HalfKPで
対駒割+528 Eloを達成した（[RESULTS.md](RESULTS.md) 参照）。
2026-07-21、オーナー判断で構造を純粋HalfKPのまま暫定フリーズした。
factorizer等の構造案はIDEAS.mdに残し、まずデータ拡大学習と探索改善を
優先する。v0.7.0タグは400M学習の結果を確認してから打つ。

## フェーズ進捗

| フェーズ | 内容 | 出口条件 | 状況 |
|---|---|---|---|
| P0 | リポジトリ基盤・横断方針 | CIが緑、ADRプロセス稼働 | 完了 |
| P1 | 盤面表現・指し手生成 | perft(5) = 19,861,490 がCIで緑 | 完了（v0.2.0） |
| P2 | USI + 探索v1（駒割評価） | GUIで対局完走、詰将棋正答 | 完了（v0.3.0） |
| P3 | 探索強化 + Lazy SMP + ルール完全対応 | NPSスケーリング確認、SPRTゲート運用開始 | 完了（v0.4.0） |
| P4 | NNUE推論 | 差分計算=全計算の一致、SIMD=スカラー一致 | 完了（v0.5.0） |
| P5 | 学習パイプライン | 自前学習ネットが駒割にSPRT有意勝ち | 完了（v0.6.0） |
| P6 | 学習基盤の完成 | データ量に対してvalid lossとEloが単調改善、ハイパラ固定 | 完了 |
| P7 | ネットワーク構造の探索・決定 | 構造をフリーズし、本格学習に移行 | 出口判定済み（純粋HalfKPで暫定フリーズ。残る構造案はIDEAS.md） |
| P8 | 本格学習 + 探索改善 + データ拡大 | floodgate参戦可能な棋力 | 先行着手（探索改善キャンペーン進行中） |

バージョン運用（ADR-0007）: フェーズ出口ごとに0.MINORを上げてタグを打つ。

## 直近の残作業

- 探索改善キャンペーン（2026-07-21オーナー決定で最優先）:
  halfkp_180M.hmwr.bestをベースに、IDEAS.mdの探索案を順に導入して
  SPRTで効果を見る。パラメータチューニングはしない。過去失敗した
  案（singular extension、SEE枝刈り）も再挑戦する。1アイデア1ADRで
  議論してから実装する
- haoデータ追加DL（024-047）は先行して取得だけ進める
- 400M学習と対halfkp_180MのSPRTは探索改善の後段。結果を見てv0.7.0タグ
- 後段のデータ拡大（gensfen自前実装、自己対局RL世代ループ）は
  IDEAS.mdとADR索引のバックログを参照。floodgate参戦がP8出口条件
- 持ち越し: x86_64でのperft・ベンチ再計測（環境入手待ち）、
  df-pn（任意）、mate1plyの探索組み込み（テーブル駆動の高速版で再挑戦予定）
